on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Doing test deploy to Pinata
    steps:
      - uses: actions/checkout@v2.1.0
      - name: Pinata deploy action step
        id: deploy
        uses: anantaramdas/ipfs-pinata-deploy-action@v1.6.4
        with:
          pin-name: 'UploadGitHubWorkflow'
          path: './.github/workflows/site2test'
          pinata-api-key: 83e2c2824b20c11c5af9
          pinata-secret-api-key: cc8893c0f90952dddc8bbd8b1a5c0ad054a1b49302f8602df48b376acb052e62
          verbose: true
          remove-old: false
      # Use the output from the `deploy` step
      - name: Get the output hash
        run: echo "The hash was ${{ steps.deploy.outputs.hash }}"